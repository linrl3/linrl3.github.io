# TODO

- Photos section:
  - Decide if you want a simple grid or a list with captions.
  - Choose where to store images:
    - `static/photos/` (simple, direct URLs)
    - `content/photos/` page bundles (albums/structured)
  - Decide layout (e.g., 2-column mobile / 4-column desktop).
  - Provide initial photos to add.
